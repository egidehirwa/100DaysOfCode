const toggle = document.getElementsByClassName('account__user-wrapper');
const userCard = document.getElementsByClassName('account-card');

toggle.onclick = function() {
    if (userCard.style.display !== "none") {
        userCard.style.display !== "none"
    } else {
        userCard.style.display !== "block"
    }
}

